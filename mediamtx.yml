# Change the source URL below to the RTSP address of your Yi camera.
hls: true
hlsAddress: :8888
hlsAllowOrigins: ['*']
hlsAlwaysRemux: true
hlsVariant: lowLatency

paths:
  yi:
    # The real URL comes from MTX_PATHS_YI_SOURCE in docker-compose.yml.
    source: publisher
    rtspTransport: tcp
